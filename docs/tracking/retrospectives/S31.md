# Sprint Retro

## 1. Sprint 信息
- Sprint: S31
- 周期: W33（已完成）
- 主持人: codex
- 参与人: codex

## 2. 数据回顾
1. Planned Points: 11
2. Completed Points: 11
3. Carry-over Points: 0
4. P0 缺陷: 0

## 3. What Went Well
1. S31 启动后即完成范围冻结，避免继续在发布运维主线上扩张工作面。
2. `smoke:postgres:e2e-local` 已补齐缺少 Docker CLI、daemon 未启动与显式外部模式 warning 三类失败语义，默认路径更易判读。
3. `rc:package:from-gate` 已完成真实 smoke，RC 验收模板、summary 副本、manifest 与 release row 可一键出包，且已纳入 `test:release-scripts` 回归。

## 4. What Went Wrong
1. tracking 治理仍要求“只要 Sprint 出现 Done Story，就要同步补 retro/metrics”，计划阶段需要更早创建占位文档。
2. `release-record-candidate` 初版使用 UTC 截断日期，真实 smoke 才暴露本地 tracking 日期漂移，需要补一轮本地日期修正与回归断言。

## 5. Root Causes
1. 当前治理模型把“开始执行”与“形成可追溯管理面”强绑定，Sprint 一旦进入执行态就必须同步补全文档骨架。
2. RC 交付包此前只有模板与 release row 自动化，且发布日期默认取 UTC，自测不足以覆盖本地台账语义。

## 6. Action Items（下个 Sprint 必做 / 状态跟踪）
1. 仅保留后续仍需推进的事项为 `Open`。
2. 已完成或失效项不删除，统一标记为 `Done` / `Obsolete`，并在 `resolution` 记录处理结果或去向。

| action | owner | due_date | related_id | status | resolution |
|---|---|---|---|---|---|
| 为 Postgres smoke 增加 daemon 就绪预检与外部连接串 FAQ 收敛 | codex | 2026-03-12 | US-11158 | Done | 已完成脚本预检、runbook/RC FAQ 与 shell 回归测试 |
| 增加基于 `release:checklist` summary 自动生成 RC 交付包目录的脚本化入口 | codex | 2026-03-12 | US-11159 | Done | 已完成 `rc:package:from-gate`、本地日期修正、真实 RC 包 smoke 与 `test:release-scripts` 接入 |
| 将 S31 完成项统一回写到 release / runbook / 风险 / metrics 台账收口记录 | codex | 2026-03-12 | US-11160 | Done | 已完成 `REL-069`、最终 `release:checklist`、risk-register、metrics/activity/backlog/sprint-board/RC 清单全量同步 |
