# Sprint Retro

## 1. Sprint 信息
- Sprint: S30
- 周期: W32（完成）
- 主持人: codex
- 参与人: codex

## 2. 数据回顾
1. Planned Points: 29
2. Completed Points: 29
3. Carry-over Points: 0
4. P0 缺陷: 0

## 3. What Went Well
1. S30 启动后，practice/retry queue 的 Postgres 仓储、迁移脚本与跨重启回归已快速落地，说明 S29 的仓储模式具有可复用性。
2. `smoke:postgres:e2e-local` 已平滑接入 practice-state、speaking-state 与 writing-state 迁移和集成测试，新增持久化模块没有脱离统一验证基线。
3. speaking session 的 resume recovery 已纳入 Postgres 集成测试，WebSocket 状态不再完全依赖单进程内存。
4. writing evaluation / rewrite archive / template usage 已具备跨重启恢复能力，内容训练链路的落库覆盖面继续扩大。
5. mock exam/report/writeback 也已纳入 Postgres 仓储与统一 smoke，内容训练主链路的服务端持久化已基本闭环。
6. 新增 `smoke:content-restart:e2e-local` 后，可稳定复现“content recovery integration + 浏览器内容主链路”的组合验证。
7. 多仓储环境变量矩阵与 smoke 说明已回写到 runbook / RC checklist，值班与本地排障说明更完整。
8. 最终 `release:checklist` 已以 strict 治理 + Postgres smoke + frontend-full-e2e 组合通过，并生成 `REL-068` 作为 S30 收口记录。

## 4. What Went Wrong
1. 多仓储变量持续增长，即使已有回退链说明，值班仍需要显式声明 backend 才能避免误判当前落库范围。
2. 为控制门禁耗时，`release:checklist` 仍未直接串联 `smoke:content-restart:e2e-local`，内容恢复验证需要按 runbook 单独执行后再跑最终门禁。

## 5. Root Causes
1. 当前持久化策略按模块分仓推进，天然带来更多环境变量、连接串继承与逐层回退路径。
2. 为保持发布门禁时长可控，综合恢复验证被设计成“专项 smoke + 最终 checklist”两层结构，而不是全部塞进默认门禁。

## 6. Action Items（下个 Sprint 必做 / 状态跟踪）
1. 仅保留后续仍需推进的事项为 `Open`。
2. 已完成或失效项不删除，统一标记为 `Done` / `Obsolete`，并在 `resolution` 记录处理结果或去向。

| action | owner | due_date | related_id | status | resolution |
|---|---|---|---|---|---|
| 推进 speaking / writing / mock 的持久化与恢复回归，避免 S30 只完成 practice 一侧 | codex | 2026-03-12 | US-11151;US-11152;US-11153 | Done | speaking / writing / mock Postgres 仓储、迁移脚本与统一 smoke 已完成 |
| 将 content recovery 集成测试与浏览器 smoke 固化为本地脚本入口 | codex | 2026-03-12 | US-11154 | Done | 已新增 `smoke:content-restart:e2e-local` 并通过验证 |
| 回写多仓储环境变量、推荐 smoke 顺序与 RC 命令矩阵 | codex | 2026-03-12 | US-11155 | Done | 已更新 `S9-Release-Runbook.md` 与 `RC-Acceptance-Checklist.md` |
| 将 S30 完成项统一回写到 release / runbook / 风险 / metrics 台账收口记录 | codex | 2026-03-12 | US-11156 | Done | 已完成 `REL-068`、最终 release checklist、risk-register、metrics/activity/backlog/sprint-board 全量同步 |
