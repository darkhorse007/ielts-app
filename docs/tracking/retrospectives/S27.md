# Sprint Retro

## 1. Sprint 信息
- Sprint: S27
- 周期: W29
- 主持人: codex
- 参与人: codex

## 2. 数据回顾
1. Planned Points: 25
2. Completed Points: 25
3. Carry-over Points: 0
4. P0 缺陷: 0

## 3. What Went Well
1. env-file 组合场景回归完善后，配置输入边界更清晰，后续改造回归风险下降。
2. schema_url 路径约束让 strict-codes 契约从“存在字段”提升到“字段格式正确”。
3. 新增台账同步模板脚本后，Sprint 收口生成速度明显提升，减少手工漏项。

## 4. What Went Wrong
1. Story ID 连续推进时若不做冲突扫描，仍可能与历史编号重叠。
2. 模板脚本首版落地后仍需要人工粘贴到多文件，尚未完全自动 apply。

## 5. Root Causes
1. 当前台账编号与内容仍由人工分配，缺少自动分配保护。
2. 模板脚本优先覆盖“生成片段”，未直接接入“安全写回”流程。

## 6. Action Items（下个 Sprint 必做 / 状态跟踪）
1. 已完成项保留在回顾中做闭环记录，不再长期悬挂为“下个 Sprint 待做”。
2. 后续仅将仍需推进的事项标记为 `Open`，已完成/失效项分别标记为 `Done`/`Obsolete`。

| action | owner | due_date | related_id | status | resolution |
|---|---|---|---|---|---|
| 在台账模板脚本中增加 Story ID 冲突预检（扫描 backlog 并阻断重号） | codex | 2026-04-19 | US-11127 | Done | 已于 2026-03-05 落地到 `scripts/tracking-sprint-sync-template.sh`，可在生成前阻断重号 |
| 为 strict-codes schema 增加 `output_mode` 与 `mode` 组合约束并补契约负向测试 | codex | 2026-04-19 | US-11125 | Done | 已于 2026-03-05 落地到 `tracking-governance-strict-codes` schema 契约测试 |
| 增加 `tracking-sprint-sync-template` 的 `--apply` 安全写回模式（含 dry-run） | codex | 2026-04-19 | US-11127 | Done | 已于 2026-03-05 支持 `--apply true` 与 `--dry_run true` 安全写回 |
