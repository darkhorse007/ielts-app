# Sprint Retro

## 1. Sprint 信息
- Sprint: S29
- 周期: W31（进行中）
- 主持人: codex
- 参与人: codex

## 2. 数据回顾
1. Planned Points: 25
2. Completed Points: 25
3. Carry-over Points: 0
4. P0 缺陷: 0

## 3. What Went Well
1. S29 启动当天即完成范围冻结、前端 `/admin` 登录保护与 system RBAC 默认收紧，安全边界先于持久化改造落地。
2. Docker daemon 拉起后，`smoke:postgres:e2e-local` 与带 Postgres 的 `release:checklist` 已通过，RC 基线验证恢复完整。
3. RBAC 默认策略切换后，已通过全量类型检查、单测与前端全链路 E2E，说明系统级回归覆盖较完整。
4. auth/account 已具备独立 Postgres 仓储、迁移脚本与跨重启回归，为后续 onboarding/progress 持久化提供了可复用底座。
5. onboarding/study-plan/progress 已复用同类 Postgres 仓储模式并纳入 `smoke:postgres:e2e-local`，S29 持久化主线从点状验证升级为统一基线。
6. 新增 `smoke:learner-restart:e2e-local` 后，团队可一条命令完成“restart integration + learner 主链路回归”的组合验证。

## 4. What Went Wrong
1. tracking 治理对“当前 Sprint 一旦出现 Done Story 就要求 retro/metrics 就绪”较严格，启动日需要同步补齐治理材料。
2. Postgres smoke 仍依赖外部 Docker / OrbStack daemon；虽然本次已打通，但环境前置条件仍需在 runbook 中持续强调。

## 5. Root Causes
1. 当前 Sprint 启动后存在“代码先落地、治理材料后补齐”的节奏差。
2. S29 的关键阻断项之一依赖环境基础设施，而不是单纯的仓库内代码逻辑。

## 6. Action Items（下个 Sprint 必做 / 状态跟踪）
1. 仅保留后续仍需推进的事项为 `Open`。
2. 已完成或失效项不删除，统一标记为 `Done` / `Obsolete`，并在 `resolution` 记录处理结果或去向。

| action | owner | due_date | related_id | status | resolution |
|---|---|---|---|---|---|
| 执行 Postgres smoke 并将 RC 签署、发布记录同步收口 | codex | 2026-03-09 | US-11142 | Done | 已完成 `smoke:postgres:e2e-local`、带 Postgres 的 `release:checklist`、`REL-066` 与 RC 表格回写 |
| 启动 auth/account/onboarding/progress repository 化设计与迁移实现 | codex | 2026-03-10 | US-11145;US-11146 | Done | auth/account 与 onboarding/progress Postgres 仓储、迁移脚本、跨重启回归和 smoke 基线均已完成 |
| 将 S29 完成项统一回写到 release / runbook / 台账收口记录 | codex | 2026-03-10 | US-11148 | Done | 已完成 `REL-067`、runbook、risk-register、metrics/activity/backlog/sprint-board 全量同步 |
