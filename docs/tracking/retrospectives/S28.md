# Sprint Retro

## 1. Sprint 信息
- Sprint: S28
- 周期: W30
- 主持人: codex
- 参与人: codex

## 2. 数据回顾
1. Planned Points: 27
2. Completed Points: 27
3. Carry-over Points: 0
4. P0 缺陷: 0

## 3. What Went Well
1. 前端运行时配置完成 env 化并补齐启动前校验后，本地开发、部署和跨域接入边界更清晰。
2. 学员主链路与内容训练主链路已进入浏览器级回归范围，候选发布前的用户视角风险显著下降。
3. Postgres 基线、RC 验收清单、已知问题模板和演示脚本已形成统一发布包，收口动作更可复用。

## 4. What Went Wrong
1. 本地 Postgres smoke 仍依赖 Docker daemon，环境未就绪时无法在当前会话直接完成默认路径验证。
2. S28 前半程以 Draft 方式推进，正式台账同步滞后于代码落地，期间需要额外做一次集中收口。

## 5. Root Causes
1. Postgres 验证依赖外部基础设施，执行成功不完全由仓库代码决定。
2. 当前 Sprint 启动与执行同步仍偏“先实现、后回填”，缺少更早的正式 tracking 接入节奏。

## 6. Action Items（下个 Sprint 必做 / 状态跟踪）
1. 仅保留后续仍需推进的事项为 `Open`。
2. 已完成或失效项不删除，统一标记为 `Done` / `Obsolete`，并在 `resolution` 记录处理结果或去向。

| action | owner | due_date | related_id | status | resolution |
|---|---|---|---|---|---|
| 为 Postgres smoke 增加 daemon 就绪预检与外部连接串示例收敛到统一 FAQ | codex | 2026-05-03 | US-11138 | Done | 已在 S31 `US-11158` 完成 `local-postgres-e2e-smoke.sh` 预检、runbook/RC FAQ 与 shell 回归测试 |
| 增加基于 `release:checklist` summary 自动生成 RC 交付包目录的脚本化入口 | codex | 2026-05-03 | US-11139 | Done | 已在 S31 `US-11159` 完成 `rc:package:from-gate`、目录级产物编排、真实 smoke 与回归测试 |
