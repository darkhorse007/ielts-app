# 客户端 API 基址；留空表示使用与页面同源的 HTTP 路径。
VITE_API_BASE_URL=

# 客户端 WebSocket 基址；留空表示从 API 基址或当前页面来源自动推导。
VITE_WS_BASE_URL=

# 仅本地 Vite 开发服务器使用：将 /v1 与 /health 代理到后端。
VITE_DEV_PROXY_TARGET=http://127.0.0.1:8787
